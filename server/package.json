{
  "name": "mini-bank-server",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --experimental-transform-types --test",
    "start": "node --env-file-if-exists=.env --experimental-transform-types ./src/main.ts",
    "deploy": "node --env-file=.env --experimental-transform-types ./scripts/deploy.ts"
  },
  "dependencies": {
    "@trpc/server": "^11.0.0-rc.748",
    "cors": "^2.8.5",
    "dayjs": "^1.11.13",
    "ioredis": "^5.5.0",
    "tinkoff-invest-api": "^7.0.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/node": "^22.13.1",
    "typescript": "^5.7.3",
    "zx": "^8.3.2"
  }
}
